#include<bits/stdc++.h>
using namespace std;
bool monotonic(vector<int> &l, vector<int> &r, int &k){
    int coord = 0;
}
void solve(){
    int n; cin >> n;
    vector<int> left(n);
    vector<int> right(n);

    for(int i = 0; i < n; i++)
        cin >> left[i] >> right[i];

}

int main(){
    int t;
    cin >> t;

    while(t--)
        solve();
}